<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="../public/img/favicon.png" type="image/x-icon">
    <title>Subscribed</title>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet">

    <style>
        * {
            margin: 0px;
            padding: 0px;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Poppins', sans-serif;
            text-align: center;
        }

        .navbar {
            background-color: #000000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px;
        }

        .navbar ul {
            display: flex;
            list-style: none;
            margin: 0;
            padding: 0;
            overflow: auto;
        }

        .navbar li {
            margin: 0.6rem;
            font-size: 20px;
        }

        .navbar li a {
            padding: 3px 13px;
            text-decoration: none;
            color: white;

        }

        .navbar li a:hover {
            color: rgb(238, 159, 84);
        }

        .search {
            position: absolute;
            right: 10px;
            color: white;
        }

        #search {
            font-family: 'Poppins', sans-serif;
            width: 80%;
            height: 5vh;
            border-radius: 5px;
        }

        input[type="text"]::placeholder {
            text-align: center;
        }

        @media screen and (max-width: 767px) {

            .search {
                color: white;
                display: none;
            }
        }

        /* Container */
        .container {
            margin: 1rem auto;
            text-align: center;
        }

        @media screen and (max-width: 767px) {
            .container {
                margin: 1rem;
            }
        }

        nav::before {
            content: "";
            background: url('https://source.unsplash.com/1600x900/?stocks') no-repeat center center/cover;
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            opacity: 0.5;

        }

        .message {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            z-index: 9999;
        }

        .message__content {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 2rem;
            text-align: center;
            max-width: 600px;
            border-radius: 10px;
            animation: slide-up 1s ease-out;
        }

        .message__text {
            font-size: 1.5rem;
            margin-bottom: 2rem;
        }

        .message__button {
            background-color: #0069d9;
            color: #fff;
            border: none;
            margin-bottom: 10px;
            padding: 0.5rem 1rem;
            font-size: 1rem;
            border-radius: 5px;
            text-decoration: none;
            cursor: pointer;
            transition: background-color 0.2s ease-out;
        }

        .message__button:hover {
            background-color: #0051a8;
        }

        @media screen and (max-width: 767px) {
            .message {
                background-size: contain;
            }

            .message__content {
                padding: 1rem;
            }
        }

        @keyframes slide-up {
            0% {
                transform: translateY(100%);
                opacity: 0;
            }

            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }

        /* Footer */
        .footer {
			display: flex;
			justify-content: center;
			background-color: #333;
			color: #fff;
			position: absolute;
            padding-top: 8px;
			bottom: 0;
            height: 5vh;
			width: 100%;
		}

		.footer__copy {
			margin: 0;
		}

		.btn-2 {
			margin-left: 10px;
			color: #4CAF50;
			text-decoration: none;
			border-bottom: 1px dotted #4CAF50;
			cursor: pointer;
		}
        .btn-2:hover {
            background-color: #068265;
        }
    </style>
</head>

<body>
    <nav class="navbar">

        <ul>
            <li><a href="/home">Home</a></li>
            <li><a href="">About</a></li>
            <li><a href="">Services</a></li>
            <li><a href="">Contact us</a></li>
            <div class="search">
                <input type="text" name="search" id="search" placeholder="Search Stock">
            </div>
        </ul>
    </nav>
    <div class="container">
        <div class="message">
            <div class="message__content">
                <p class="message__text">Thank you for subscribing to our service! You will now receive real-time alerts whenever a stock in NIFTY-50 crashes. Stay ahead of the game and make informed decisions.</p>
                <a class="message__button" href="/home">Close</a>
            </div>
        </div>
    </div>
    <footer class="footer">
        <p class="footer__copy">&#169; Pushkar Helge, All rights reserved.</p>
        {{!-- <a class="btn-2" href="/unsubscribe">Unsubscribe</a> --}}
    </footer>


</body>

</html>